#!/bin/bash
BRANCHES=$(git branch --merged | grep -E -v '(^\*|master)')
[[ -z "${BRANCHES}" ]] && {
  echo "No branch to delete"
  exit
}
echo "Confirm deletion of the following branches:"
echo "${BRANCHES}"
echo -n "y/n "
read -r answer
[[ "${answer}" = 'y' ]] && {
  git branch -d ${BRANCHES}
}
